import React from "react";
import SocialLinks from "./SocialLinks";

const Hero = () => {
  return (
    <div className="relative w-screen flex-grow bg-gray-200" id="hero">
      <SocialLinks />
    </div>
  );
};

export default Hero;
